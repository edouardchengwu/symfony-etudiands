{% extends 'base.html.twig' %}
{% block title %}Modifier {{ etudiant.nomComplet }}{% endblock %}
{% block body %}
<div class="d-flex align-items-center gap-2 mb-3">
    <a href="{{ path('etudiant_show', {id: etudiant.id}) }}" class="btn btn-sm btn-light"><i class="bi bi-arrow-left"></i></a>
    <h3 class="fw-bold mb-0"><i class="bi bi-pencil-square"></i> Modifier {{ etudiant.nomComplet }}</h3>
</div>
<div class="card p-4">
    {{ include('etudiant/_form.html.twig') }}
</div>
{% endblock %}
